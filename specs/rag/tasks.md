---

description: "Task list template for feature implementation"
---

# Tasks: RAG Chatbot

**Input**: Design documents from `/specs/rag/`
**Prerequisites**: plan.md (required), spec.md (required for user stories)

**Tests**: The examples below include test tasks. Tests are OPTIONAL - only include them if explicitly requested in the feature specification.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Path Conventions

- **Single project**: `src/`, `tests/` at repository root
- **Web app**: `backend/src/`, `frontend/src/`
- **Mobile**: `api/src/`, `ios/src/` or `android/src/`
- Paths shown below assume single project - adjust based on plan.md structure

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [x] T001 [P] Create `backend` and `frontend` directories if they don't exist.
- [x] T002 [P] Initialize a Python project with FastAPI in the `backend` directory.
- [x] T003 [P] Initialize a Docusaurus project in the `frontend` directory.
- [x] T004 [P] Configure linting and formatting tools for both Python (e.g., `black`, `ruff`) and TypeScript (e.g., `eslint`, `prettier`).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T005 [P] Set up API routing and middleware structure in the FastAPI application (`backend/src/main.py` and `backend/src/core/middleware.py`).
- [x] T006 [P] Implement environment configuration management for both `backend` and `frontend` to handle API keys and other secrets (e.g., using `.env` files and `python-dotenv` for backend, and Docusaurus's `customFields` for frontend in `docusaurus.config.ts`).
- [x] T007 [P] Configure error handling and logging infrastructure for the FastAPI application.
- [x] T008 [P] Establish a connection to Qdrant Cloud from the backend (`backend/src/core/qdrant.py`).
- [x] T009 [P] Establish a connection to NeonDB from the backend (`backend/src/core/neondb.py`).

**Checkpoint**: Foundation ready - user story implementation can now begin in parallel

---

## Phase 3: User Story 1 - User asks a question and gets an answer (Priority: P1) 🎯 MVP

**Goal**: A user can ask a question in the chatbot interface and receive an answer generated by an AI, with cited sources from the textbook.

### Tests for User Story 1 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T010 [P] [US1] Write an integration test for the `/rag/query` endpoint in `backend/tests/integration/test_rag_api.py`.
- [x] T011 [P] [US1] Write a unit test for the RAG service in `backend/tests/unit/test_rag_service.py`.
- [x] T012 [P] [US1] Write a component test for the `RAGChatbot.tsx` component in `frontend/tests/components/RAGChatbot.test.tsx`.

### Implementation for User Story 1

- [x] T013 [P] [US1] Create the data models for the `/rag/query` request and response in `backend/src/models/rag_models.py`.
- [x] T014 [US1] Implement the RAG service in `backend/src/services/rag_service.py` to handle the logic of retrieving content from Qdrant and generating an answer with an LLM (e.g., OpenAI).
- [x] T015 [US1] Implement the `/rag/query` endpoint in `backend/src/api/rag.py`.
- [x] T016 [P] [US1] Create the `RAGChatbot.tsx` component in `frontend/src/components/RAGChatbot.tsx` with Chat Input Area, Chat History Display, Loading Indicator, and Error Message Display as per spec.
- [x] T017 [P] [US1] Create an API client in `frontend/src/services/rag_api_client.ts` to communicate with the backend `/rag/query` endpoint.
- [x] T018 [US1] Integrate the `RAGChatbot.tsx` component into a Docusaurus page (e.g., `frontend/src/pages/rag-chatbot.tsx`) and connect it to the backend API client.
- [x] T019 [US1] Add validation and error handling for the `/rag/query` endpoint (backend).
- [x] T020 [US1] Add logging for the `/rag/query` endpoint operations (backend).
- [x] T021 [US1] Implement the loading indicator visual feedback in `RAGChatbot.tsx`.
- [x] T022 [US1] Implement displaying sources from the backend response in `RAGChatbot.tsx`.

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently

---

## Phase 4: User Story 2 - System embeds textbook content (Priority: P1)

**Goal**: The system can process textbook content, convert it into embeddings, and store them in the Qdrant vector database.

### Tests for User Story 2 ⚠️

- [x] T023 [P] [US2] Write an integration test for the `/embeddings/upsert` endpoint in `backend/tests/integration/test_rag_api.py`.
- [x] T024 [P] [US2] Write a unit test for the embedding service in `backend/tests/unit/test_rag_service.py`.

### Implementation for User Story 2

- [x] T025 [P] [US2] Create the data models for the `/embeddings/upsert` request and response in `backend/src/models/rag_models.py`.
- [x] T026 [US2] Implement the embedding service in `backend/src/services/rag_service.py` to handle the logic of processing content, generating embeddings with OpenAI's `text-embedding-3-small` model, and upserting them to Qdrant with specified metadata and schema.
- [x] T027 [US2] Implement the `/embeddings/upsert` endpoint in `backend/src/api/rag.py`.
- [x] T028 [US2] Add validation and error handling for the `/embeddings/upsert` endpoint (backend).
- [x] T029 [US2] Add logging for the `/embeddings/upsert` endpoint operations (backend).

**Checkpoint**: At this point, User Stories 1 AND 2 should both work independently

---

## Phase 5: User Story 3 - User can see their chat history (Priority: P2)

**Goal**: A user can see their previous questions and the answers they received from the chatbot.

### Tests for User Story 3 ⚠️

- [x] T030 [P] [US3] Write a component test for the chat history display in `frontend/tests/components/RAGChatbot.test.tsx`.

### Implementation for User Story 3

- [x] T031 [US3] Implement state management for chat history in the `RAGChatbot.tsx` component.
- [x] T032 [US3] Update the `RAGChatbot.tsx` component to render the chat history in a scrollable container, maintaining chronological order.

**Checkpoint**: All user stories should now be independently functional

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [x] T033 [P] Add comprehensive documentation to the `RAGChatbot.tsx` component and the backend API endpoints.
- [x] T034 Refactor and clean up the code in both the `frontend` and `backend` to improve readability and maintainability.
- [ ] T035 Optimize the performance of the `/rag/query` endpoint by fine-tuning the Qdrant search parameters and the LLM prompt.
- [x] T035 [P] Add additional unit and integration tests to increase code coverage.
- [x] T036 Harden the security of the backend API by implementing rate limiting and more robust input validation.
- [x] T038 Validate the end-to-end functionality of the RAG chatbot by following the steps in `specs/rag/quickstart.md`.
- [ ] T039 Implement edge case handling for "no relevant information found" in `RAGChatbot.tsx`.
- [ ] T040 Implement edge case handling for "inappropriate or offensive questions" in the backend and `RAGChatbot.tsx`.
- [ ] T041 Implement edge case handling for LLM failure in `RAGChatbot.tsx`.
- [ ] T042 Implement edge case handling for Qdrant/OpenAI service unavailability or errors in the backend and `RAGChatbot.tsx`.