---

description: "Task list template for feature implementation"
---

# Tasks: RAG Chatbot

**Input**: Design documents from `/specs/001-rag-chatbot/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/

**Tests**: The examples below include test tasks. Tests are OPTIONAL - only include them if explicitly requested in the feature specification.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Path Conventions

- **Single project**: `src/`, `tests/` at repository root
- **Web app**: `backend/src/`, `frontend/src/`
- **Mobile**: `api/src/`, `ios/src/` or `android/src/`
- Paths shown below assume single project - adjust based on plan.md structure

<!-- 
  ============================================================================
  IMPORTANT: The tasks below are SAMPLE TASKS for illustration purposes only.
  
  The /sp.tasks command MUST replace these with actual tasks based on:
  - User stories from spec.md (with their priorities P1, P2, P3...)
  - Feature requirements from plan.md
  - Entities from data-model.md
  - Endpoints from contracts/
  
  Tasks MUST be organized by user story so each story can be:
  - Implemented independently
  - Tested independently
  - Delivered as an MVP increment
  
  DO NOT keep these sample tasks in the generated tasks.md file.
  ============================================================================
-->

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [ ] T001 Create `backend` and `frontend` directories.
- [ ] T002 Initialize a Python project with FastAPI in the `backend` directory.
- [ ] T003 Initialize a Docusaurus project in the `frontend` directory.
- [ ] T004 [P] Configure linting and formatting tools for both Python (e.g., `black`, `ruff`) and TypeScript (e.g., `eslint`, `prettier`).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [ ] T005 Set up API routing and middleware structure in the FastAPI application (`backend/src/api/`).
- [ ] T006 Implement environment configuration management for both `backend` and `frontend` to handle API keys and other secrets (e.g., using `.env` files and `python-dotenv` for backend, and Docusaurus's `customFields` for frontend).
- [ ] T007 Configure error handling and logging infrastructure for the FastAPI application.
- [ ] T008 Establish a connection to Qdrant Cloud from the backend (`backend/src/core/qdrant.py`).
- [ ] T009 Establish a connection to NeonDB from the backend (`backend/src/core/neondb.py`).

**Checkpoint**: Foundation ready - user story implementation can now begin in parallel

---

## Phase 3: User Story 1 - User asks a question and gets an answer (Priority: P1) 🎯 MVP

**Goal**: A user can ask a question in the chatbot interface and receive an answer generated by an AI, with cited sources from the textbook.

**Independent Test**: This can be tested by inputting a question into the chatbot and verifying that an answer is returned, is relevant, and includes verifiable sources.

### Tests for User Story 1 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [ ] T010 [P] [US1] Write an integration test for the `/rag/query` endpoint in `backend/tests/integration/test_rag_api.py`.
- [ ] T011 [P] [US1] Write a unit test for the RAG service in `backend/tests/unit/test_rag_service.py`.
- [ ] T012 [P] [US1] Write a component test for the `RAGChatbot.tsx` component in `frontend/tests/components/RAGChatbot.test.tsx`.

### Implementation for User Story 1

- [ ] T013 [P] [US1] Create the data models for the `/rag/query` request and response in `backend/src/models/rag_models.py`.
- [ ] T014 [US1] Implement the RAG service in `backend/src/services/rag_service.py` to handle the logic of retrieving content from Qdrant and generating an answer with OpenAI.
- [ ] T015 [US1] Implement the `/rag/query` endpoint in `backend/src/api/rag.py`.
- [ ] T016 [P] [US1] Create the `RAGChatbot.tsx` component in `frontend/src/components/RAGChatbot.tsx`.
- [ ] T017 [P] [US1] Create an API client in `frontend/src/services/rag_api_client.ts` to communicate with the backend.
- [ ] T018 [US1] Integrate the `RAGChatbot.tsx` component into a Docusaurus page and connect it to the backend API client.
- [ ] T019 [US1] Add validation and error handling for the `/rag/query` endpoint.
- [ ] T020 [US1] Add logging for the `/rag/query` endpoint operations.

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently

---

## Phase 4: User Story 2 - System embeds textbook content (Priority: P1)

**Goal**: The system can process textbook content, convert it into embeddings, and store them in the Qdrant vector database.

**Independent Test**: This can be tested by adding or updating a chapter in the textbook, triggering the embedding process, and then verifying that the new content is present in the Qdrant vector database.

### Tests for User Story 2 ⚠️

- [ ] T021 [P] [US2] Write an integration test for the `/embeddings/upsert` endpoint in `backend/tests/integration/test_rag_api.py`.
- [ ] T022 [P] [US2] Write a unit test for the embedding service in `backend/tests/unit/test_rag_service.py`.

### Implementation for User Story 2

- [ ] T023 [P] [US2] Create the data models for the `/embeddings/upsert` request and response in `backend/src/models/rag_models.py`.
- [ ] T024 [US2] Implement the embedding service in `backend/src/services/rag_service.py` to handle the logic of processing content, generating embeddings with OpenAI, and upserting them to Qdrant.
- [ ] T025 [US2] Implement the `/embeddings/upsert` endpoint in `backend/src/api/rag.py`.
- [ ] T026 [US2] Add validation and error handling for the `/embeddings/upsert` endpoint.
- [ ] T027 [US2] Add logging for the `/embeddings/upsert` endpoint operations.

**Checkpoint**: At this point, User Stories 1 AND 2 should both work independently

---

## Phase 5: User Story 3 - User can see their chat history (Priority: P2)

**Goal**: A user can see their previous questions and the answers they received from the chatbot.

**Independent Test**: This can be tested by asking multiple questions to the chatbot and verifying that the chat history is displayed correctly in the chatbot interface.

### Tests for User Story 3 ⚠️

- [ ] T028 [P] [US3] Write a component test for the chat history display in `frontend/tests/components/RAGChatbot.test.tsx`.

### Implementation for User Story 3

- [ ] T029 [US3] Implement state management for chat history in the `RAGChatbot.tsx` component.
- [ ] T030 [US3] Update the `RAGChatbot.tsx` component to render the chat history.
- [ ] T031 [US3] Implement a scrollable container for the chat history.

**Checkpoint**: All user stories should now be independently functional

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [ ] T032 [P] Add comprehensive documentation to the `RAGChatbot.tsx` component and the backend API endpoints.
- [ ] T033 Refactor and clean up the code in both the `frontend` and `backend` to improve readability and maintainability.
- [ ] T034 Optimize the performance of the `/rag/query` endpoint by fine-tuning the Qdrant search parameters and the LLM prompt.
- [ ] T035 [P] Add additional unit and integration tests to increase code coverage.
- [ ] T036 Harden the security of the backend API by implementing rate limiting and more robust input validation.
- [ ] T037 Validate the end-to-end functionality of the RAG chatbot by following the steps in `specs/rag/quickstart.md`.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories
- **User Stories (Phase 3+)**: All depend on Foundational phase completion
  - User stories can then proceed in parallel (if staffed)
  - Or sequentially in priority order (P1 → P2 → P3)
- **Polish (Final Phase)**: Depends on all desired user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) - No dependencies on other stories
- **User Story 2 (P2)**: Can start after Foundational (Phase 2) - May integrate with US1 but should be independently testable
- **User Story 3 (P3)**: Can start after Foundational (Phase 2) - May integrate with US1/US2 but should be independently testable

### Within Each User Story

- Tests (if included) MUST be written and FAIL before implementation
- Models before services
- Services before endpoints
- Core implementation before integration
- Story complete before moving to next priority

### Parallel Opportunities

- All Setup tasks marked [P] can run in parallel
- All Foundational tasks marked [P] can run in parallel (within Phase 2)
- Once Foundational phase completes, all user stories can start in parallel (if team capacity allows)
- All tests for a user story marked [P] can run in parallel
- Models within a story marked [P] can run in parallel
- Different user stories can be worked on in parallel by different team members

---

## Parallel Example: User Story 1

```bash
# Launch all tests for User Story 1 together (if tests requested):
Task: "Contract test for [endpoint] in tests/contract/test_[name].py"
Task: "Integration test for [user journey] in tests/integration/test_[name].py"

# Launch all models for User Story 1 together:
Task: "Create [Entity1] model in src/models/[entity1].py"
Task: "Create [Entity2] model in src/models/[entity2].py"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL - blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test User Story 1 independently
5. Deploy/demo if ready

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready
2. Add User Story 1 → Test independently → Deploy/Demo (MVP!)
3. Add User Story 2 → Test independently → Deploy/Demo
4. Add User Story 3 → Test independently → Deploy/Demo
5. Each story adds value without breaking previous stories

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together
2. Once Foundational is done:
   - Developer A: User Story 1
   - Developer B: User Story 2
   - Developer C: User Story 3
3. Stories complete and integrate independently

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- Each user story should be independently completable and testable
- Verify tests fail before implementing
- Commit after each task or logical group
- Stop at any checkpoint to validate story independently
- Avoid: vague tasks, same file conflicts, cross-story dependencies that break independence
